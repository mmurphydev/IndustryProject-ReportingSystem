<div class="row" style="text-align: center">
    <!-- Reports less than 24hours old-->
    <div class="col-md-4 text-white bg-dark">
        <h1>Today</h1><br>
            <div id="todaysPosts" class="text-white">
            </div>
    </div>
    <!-- Reports between 1 and 7 days old-->
    <div class="col-md-4">
        <h1>This Week</h1><br>
        <div id="weeklyPosts">
        </div>
    </div>
    <!-- Reports Greater than 7 days old-->
    <div class="col-md-4 text-white bg-dark">
        <h1>Older</h1><br>
        <div id="OlderPosts">
        </div>
    </div>  
</div>

<!--Model to expand image on click-->
 <div id="id01" class="modal">
    <form class="modal-content animate">
      <div class="imgcontainer">
        <span onclick="document.getElementById('id01').style.display='none'" class="close"
          title="Close Modal">&times;</span>
        <div class="container" id="innerimage">
        </div><br><br><br><br><br><br><br><br><br><br>
      </div>
    </form>
</div>

<script>
    
      $("#todaysPosts").click(function (event) {
        if (event.target.name=="img"){
        var targetArray = event.target.name
        console.log(targetArray);

        var innerImage = "<img src= '" + event.target.src + "' style='width: 100%; height: 100%'>"

        //var innerImage= "<h1>"+event.target.src+"</h1>";
        $("#innerimage").html(innerImage);
      }}
      );

      var str = "document.getElementById('id01').style.display='block'";
      // Get the modal
      var modal = document.getElementById('id01');

      // When the user clicks anywhere outside of the modal, close it
      window.onclick = function (event) {
        if (event.target == modal) {
          modal.style.display = "none";
        }
      }
    </script>

    
    <!--    
    file which will contain all of the client side
JavaScript that will be used to make 
asynchronous calls to server side APIs-->
<script src="/javascripts/feed.js"></script>

